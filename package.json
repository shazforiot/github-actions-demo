{
  "name": "github-actions-demo",
  "version": "1.0.0",
  "description": "Demo project for GitHub Actions tutorial",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "build": "echo 'Building project...' && mkdir -p dist && cp -r src/* dist/",
    "lint": "echo 'Linting code...' && exit 0"
  },
  "keywords": ["github-actions", "ci-cd", "demo"],
  "author": "",
  "license": "MIT",
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
